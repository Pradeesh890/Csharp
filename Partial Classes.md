Partial Classes is class that splits into multiple files.
Each class file is treated as a "Part of the class"

here a single class is written in multiple files but all those partial classes are merged into single class

we can create partial class using partial keyword before the modifier of the classes

eg
public partial class Product{
//code block
}


write the remaining code in the separate file with same class name and partial keyword
