Encapsulation is a concept of bundling the data (fields) and operations (methods) that manipulate the data together this hides the internal implementation details of an object and provide a essential member to interacting with them.

this encourages to use private fields and public properties or public methods

the main benefits of encapsulation is 
1. modularity
2. hiding the implementation details
3. data integrity

