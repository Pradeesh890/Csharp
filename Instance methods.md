Instance methods are associated with objects

these instance methods are mainly used for manipulating the instance fields

the methods that are declared without static keyword are called as instance methods

these methods are accessible with object through a reference variable

but these instance methods can access both instance fields and static fields

these can access both instance methods and static methods

can use "this" keyword, as there must be "current object" to call instance method.
